# -----------------------------------------------------------------------
# Runtime – required to run the trading bot
# -----------------------------------------------------------------------
ccxt==4.5.30
pandas==2.3.3
numpy==2.4.0
TA-Lib==0.6.8
python-dotenv==1.2.1
catboost==1.2.8
scikit-learn==1.8.0

# -----------------------------------------------------------------------
# LLM inference – required only when USE_ML_MODEL=true (predict.py)
# -----------------------------------------------------------------------
torch==2.9.1
transformers==4.57.3
peft==0.18.0

# -----------------------------------------------------------------------
# LLM fine-tuning – required only to retrain the LLM
# (train_trading_model.py)
# -----------------------------------------------------------------------
trl==0.26.2
datasets==4.4.2
accelerate==1.12.0
bitsandbytes==0.49.0

# -----------------------------------------------------------------------
# Development / testing
# -----------------------------------------------------------------------
pytest>=8.0
