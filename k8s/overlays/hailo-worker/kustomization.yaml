apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Base resources
resources:
  - ../../base
  - pvc-nvme.yaml
  - daemonset-hailo.yaml

namespace: trading-bot

commonLabels:
  app: trading-bot
  component: hailo-worker
  tier: edge

commonAnnotations:
  node-affinity: hailo-only
